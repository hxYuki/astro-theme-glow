---

---


<div class="hamburger hidden flex flex-col justify-between text-default">
    <span class="line"></span>
    <span class="line"></span>
    <span class="line"></span>
</div>
<script>
    document.addEventListener('astro:page-load', () => {
    document.querySelector('.hamburger')?.addEventListener('click', () => {
        document.querySelector('.nav-links')?.classList.toggle('expanded');
    });
});
</script>
<style>
    .hamburger {
        padding: 1.25rem;
        width: 1.25rem;
        height: 0.775rem;
        box-sizing: unset;        
    }

    .hamburger .line {
        background: hsl(var(--twc-default));
        display: inline-block;
        height: 0.125rem;
        border-radius: 0.0625rem;
        /* left: 0; */
        /* position: relative; */
        /* top: 0; */
        transition: all 0.4s;
        width: 100%;
        box-shadow: 0 0 0.5rem rgba(0, 0, 0, 0.5);
    }
</style>
